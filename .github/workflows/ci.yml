name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  python-ci:
    uses: stillsystems/.github/.github/workflows/reusable-python-ci.yml@main
    with:
      python-version: '3.12'
